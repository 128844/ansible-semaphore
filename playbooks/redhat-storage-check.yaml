---
- name: Check storage usage on servers
  hosts: all
  become: true
  tasks:

    - name: Check disk usage
      command: df -h
      register: disk_usage

    - name: Display disk usage
      debug:
        var: disk_usage.stdout_lines